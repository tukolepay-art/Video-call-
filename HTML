<div id="tg-wrapper">
  <video id="main-video" autoplay loop muted>
    <source src="demo-person.mp4" type="video/mp4">
  </video>

  <div class="tg-header">
    <div class="user-info">
      <span class="name">Durov</span>
      <span class="status">04:20</span>
    </div>
  </div>

  <div class="tg-footer">
    <button class="icon-btn">🔈</button>
    <button class="icon-btn video-toggle">📹</button>
    <button class="icon-btn mic-toggle">🎤</button>
    <button class="icon-btn end-call">📞</button>
  </div>
</div>

<style>
  body { margin: 0; background: #0e1621; font-family: 'Open Sans', sans-serif; }
  #tg-wrapper { position: relative; width: 100vw; height: 100vh; display: flex; flex-direction: column; }
  #main-video { width: 100%; height: 100%; object-fit: cover; }
  .tg-header { position: absolute; top: 0; width: 100%; background: linear-gradient(to bottom, rgba(0,0,0,0.6), transparent); padding: 20px; color: white; }
  .name { display: block; font-weight: bold; font-size: 18px; }
  .status { font-size: 14px; opacity: 0.8; }
  .tg-footer { position: absolute; bottom: 40px; left: 50%; transform: translateX(-50%); display: flex; gap: 25px; align-items: center; }
  .icon-btn { width: 55px; height: 55px; border-radius: 50%; border: none; background: rgba(255,255,255,0.1); color: white; font-size: 22px; cursor: pointer; backdrop-filter: blur(8px); }
  .end-call { background: #e53935; width: 70px; height: 70px; }
</style>
